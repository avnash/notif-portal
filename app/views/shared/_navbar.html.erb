<nav class="navbar navbar-collapse navbar_main " role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "S", root_path, class:"navbar-brand brand-mini"%>
      <%= link_to "Feedback Home", root_path, class:"navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if signed_in? %>
          <li><%= link_to "Give Feedback", new_post_path %></li>
          <li> <%= link_to "My Feedback", complaints_path %></li>
        <% if current_user.usertype %>
        <li><%= link_to "Edit My Tag", tag_path(id:current_user.usertype) %></li>
      <%end%>
      <li class="dropdown">
        <a href="#" data-toggle="dropdown"> </span> Categories <b class="caret"></b></a>
        <ul class="dropdown-menu signin_div">
          <li >
          <%= link_to "All <span class='badge pull-right'>#{Post.all.length}</span>".html_safe, root_path%>
        </li>
          <% for tag in Tag.all %>
          <li  data-container="body" data-trigger="hover" data-toggle="popover" data-content='<%= tag.description  %>' data-placement="left" >
            <%= link_to "#{tag.name.capitalize} <span class='badge pull-right'>#{Tag.includes(:posts).find(tag.id).posts.where("solved=?",false).length}</span>".html_safe, display_path(id:tag.id) %>
          </li>
          <% end %>
          <li>
            <%= link_to "Resolved Complaints<span class='badge pull-right'>#{Post.where("solved=?",true).length}</span>".html_safe, solved_path %>
          </li>
        </ul>
      </li>
      <% end %>
        </ul>
    </div>
  </nav>
